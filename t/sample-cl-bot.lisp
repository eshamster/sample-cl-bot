(in-package :cl-user)
(defpackage sample-cl-bot-test
  (:use :cl
        :sample-cl-bot
        :prove))
(in-package :sample-cl-bot-test)

;; NOTE: To run this test file, execute `(asdf:test-system :sample-cl-bot)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
